Select *  from Car;

Select City from Person INNER JOIN Agency ON Person.City = Agency.City;

Select Last_name, First_name from person WHERE Budget > 150;

Select Marque From Car INNER JOIN Person ON Car.Availablity = Person.Term;